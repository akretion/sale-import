<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id ="exc_inconsistent_tax_amts" model="exception.rule">
        <field name="name">Inconsistent amounts</field>
        <field name="description">Expected and imported amounts should be the same</field>
        <field name="sequence">50</field>
        <field name="model">sale.order</field>
        <field name="code">failed = not obj.check_amounts_consistency()</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
